ReactJS:
-WindowComponent
  -NavBar
  -ScoreBox
    -ScoreSearch
    -ScoreFilter
    -ScoreList
      -Score {name, score, date}
    -ScoreLength
  -AboutBox
  -MediaBox
    -VideoBox
    -PictureBox

MongoDB:
{_objectID, id, name, score, date, difficulty?}
